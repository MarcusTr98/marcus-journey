"use client";
import { milestones } from "@/data/milestones";
export default function QuickProfile({onClose}:{onClose:()=>void}){return <section className="quick-profile" aria-label="Marcus Tran quick profile"><header><div><span className="kicker">QUICK PROFILE</span><h2>Production mind.<br/>Digital builder.</h2></div><button onClick={onClose} aria-label="Close quick profile">CLOSE ×</button></header><div className="profile-intro"><p>I understand the factory floor.<br/>I improve the process.<br/>I build the system.<br/>I teach others to use it.</p><div><strong>MARCUS TRAN</strong><span>Production • Kaizen • Technology</span><a href="mailto:hello@marcusjourney.dev">hello@marcusjourney.dev ↗</a></div></div><div className="profile-grid">{milestones.map((m,i)=><article key={m.id}><span>{String(i+1).padStart(2,"0")} / {m.period}</span><h3>{m.title}</h3><strong>{m.role}</strong><p>{m.summary}</p><ul>{m.highlights.map(h=><li key={h}>{h}</li>)}</ul></article>)}</div></section>}
